# Graphql backend for sd.lk

## To use locally

- Create `.env` from `.env.sample`
- Start dev server: `yarn dev`
- Start in production mode `yarn pro`

## Deploy to now

- Create `.env.serverless` from `.env.serverless.sample`
- Run `yarn deploy`
